pandas>=1.2.3
Pillow>=8.1.2
requests>=2.31
# Imported directly (urllib3.util.retry.Retry), not just via requests. 2.0+ for Retry(backoff_jitter=...).
urllib3>=2.0
aiohttp>=3.7.4
backoff>=1.10.0
numpy>=1.21
streetlevel>=0.12.11,<0.13
